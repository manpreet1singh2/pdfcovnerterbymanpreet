'use client'

import { useEffect, useRef, useState } from 'react'
import { ArrowRight, Check, Download, FileText, LockKeyhole, Menu, RefreshCw, ShieldCheck, Sparkles, Upload, X } from 'lucide-react'

const MAX_FILE_SIZE = 20 * 1024 * 1024
type Status = 'idle' | 'ready' | 'converting' | 'success' | 'error'
type ConverterProps = { status: Status; file: File | null; error: string; downloadUrl: string; dragging: boolean; setDragging: (value: boolean) => void; chooseFile: (file?: File) => void; convert: () => void; reset: () => void; inputRef: React.RefObject<HTMLInputElement | null> }
function formatSize(bytes: number) { return `${(bytes / (1024 * 1024)).toFixed(1)} MB` }

export function Pdf2DocApp() {
  const inputRef = useRef<HTMLInputElement>(null)
  const [status, setStatus] = useState<Status>('idle'); const [file, setFile] = useState<File | null>(null); const [error, setError] = useState(''); const [downloadUrl, setDownloadUrl] = useState(''); const [dragging, setDragging] = useState(false); const [menuOpen, setMenuOpen] = useState(false)
  const chooseFile = (selected?: File) => { if (!selected) return; if (downloadUrl) URL.revokeObjectURL(downloadUrl); setError(''); setDownloadUrl(''); if (selected.type !== 'application/pdf' && !selected.name.toLowerCase().endsWith('.pdf')) { setStatus('error'); setFile(null); setError('Please choose a PDF file.'); return }; if (selected.size > MAX_FILE_SIZE) { setStatus('error'); setFile(null); setError('That PDF is larger than 20 MB.'); return }; if (selected.size === 0) { setStatus('error'); setFile(null); setError('This PDF appears to be empty.'); return }; setFile(selected); setStatus('ready') }
  const convert = async () => { if (!file || status === 'converting') return; setStatus('converting'); setError(''); try { const body = new FormData(); body.append('file', file); const response = await fetch('/api/convert', { method: 'POST', body }); if (!response.ok) { const data = await response.json().catch(() => ({})); throw new Error(data.error || 'We could not convert that PDF.') }; setDownloadUrl(URL.createObjectURL(await response.blob())); setStatus('success') } catch (cause) { setError(cause instanceof Error ? cause.message : 'Something went wrong.'); setStatus('error') } }
  const reset = () => { if (downloadUrl) URL.revokeObjectURL(downloadUrl); setFile(null); setDownloadUrl(''); setError(''); setStatus('idle') }
  useEffect(() => () => { if (downloadUrl) URL.revokeObjectURL(downloadUrl) }, [downloadUrl])
  return <div className="min-h-screen bg-background text-foreground"><header className="mx-auto flex max-w-6xl items-center justify-between px-6 py-6 lg:px-8"><a href="#top" className="flex items-center gap-3 font-semibold tracking-tight"><span className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><FileText className="size-5" /></span><span className="text-lg">PDF<span className="text-primary">2</span>DOC</span></a><nav className={`${menuOpen ? 'flex' : 'hidden'} absolute left-6 right-6 top-20 z-10 flex-col gap-4 rounded-2xl border border-border bg-card p-5 text-sm text-muted-foreground shadow-lg md:static md:flex md:flex-row md:gap-8 md:border-0 md:bg-transparent md:p-0 md:shadow-none`}><a href="#how-it-works" onClick={() => setMenuOpen(false)}>How it works</a><a href="#features" onClick={() => setMenuOpen(false)}>Features</a><a href="#privacy" onClick={() => setMenuOpen(false)}>Privacy</a></nav><button type="button" className="rounded-full border border-border p-2 md:hidden" aria-label={menuOpen ? 'Close menu' : 'Open menu'} aria-expanded={menuOpen} onClick={() => setMenuOpen((open) => !open)}><Menu className="size-5" /></button></header><main id="top"><section className="mx-auto grid max-w-6xl gap-12 px-6 pb-20 pt-14 lg:grid-cols-[1fr_0.9fr] lg:items-center lg:px-8 lg:pb-28 lg:pt-20"><div className="flex flex-col gap-7"><div className="flex w-fit items-center gap-2 rounded-full border border-primary/20 bg-primary/5 px-3 py-1.5 text-xs font-medium text-primary"><Sparkles className="size-3.5" /> Document conversion, simplified</div><h1 className="max-w-xl text-balance font-serif text-5xl leading-[1.02] tracking-tight sm:text-6xl lg:text-7xl">Your PDFs, now <em className="text-primary">editable.</em></h1><p className="max-w-lg text-pretty text-lg leading-8 text-muted-foreground">Turn PDF files into clean, editable Word documents in seconds. No sign-up, no clutter, no compromise.</p><div className="flex flex-wrap items-center gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><Check className="size-4 text-primary" /> Up to 20 MB</span><span className="flex items-center gap-2"><Check className="size-4 text-primary" /> No account needed</span></div></div><ConverterCard {...{ status, file, error, downloadUrl, dragging, setDragging, chooseFile, convert, reset, inputRef }} /></section><section id="how-it-works" className="border-y border-border bg-card/60"><div className="mx-auto grid max-w-6xl gap-8 px-6 py-16 md:grid-cols-3 lg:px-8">{[['01', 'Drop your PDF', 'Drag a file into the converter or browse your device.'], ['02', 'We do the work', 'Our converter extracts your text and rebuilds the document.'], ['03', 'Download & edit', 'Open your new DOCX in Word, Pages, or Google Docs.']].map(([number, title, description]) => <div key={number} className="flex gap-4"><span className="font-mono text-sm text-primary">{number}</span><div className="flex flex-col gap-2"><h2 className="font-semibold">{title}</h2><p className="text-sm leading-6 text-muted-foreground">{description}</p></div></div>)}</div></section><section id="features" className="mx-auto max-w-6xl px-6 py-20 lg:px-8"><div className="mb-10 flex flex-col gap-3"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-primary">Built for focus</p><h2 className="font-serif text-4xl tracking-tight">Simple by design. Useful by default.</h2></div><div className="grid gap-4 md:grid-cols-3"><Feature icon={<ShieldCheck />} title="Private by default" copy="Files are processed in memory and removed after conversion. Your documents stay yours." /><Feature icon={<RefreshCw />} title="Keeps your flow" copy="No account, no onboarding, no distractions. Upload, convert, and keep moving." /><Feature icon={<FileText />} title="Genuinely editable" copy="Get a real DOCX file you can update, share, and use in your existing workflow." /></div></section><section id="privacy" className="bg-primary px-6 py-16 text-primary-foreground"><div className="mx-auto flex max-w-4xl flex-col items-start gap-5 md:flex-row md:items-center md:justify-between"><div className="flex max-w-xl flex-col gap-3"><h2 className="font-serif text-3xl">Your documents deserve discretion.</h2><p className="leading-7 text-primary-foreground/75">We do not store your files. Every conversion is request-scoped, and the result is delivered directly to you.</p></div><LockKeyhole className="hidden size-14 opacity-30 md:block" /></div></section></main><footer className="mx-auto flex max-w-6xl flex-col gap-3 px-6 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-8"><span>© 2026 PDF2DOC</span><span>Made for documents that move.</span></footer></div>
}

function ConverterCard({ status, file, error, downloadUrl, dragging, setDragging, chooseFile, convert, reset, inputRef }: ConverterProps) { return <div className="rounded-[2rem] border border-border bg-card p-3 shadow-[0_24px_80px_-32px_hsl(var(--primary)/.35)]"><div role="button" tabIndex={status === 'idle' ? 0 : -1} aria-label="Upload a PDF file" className={`relative flex min-h-[360px] flex-col items-center justify-center rounded-[1.5rem] border-2 border-dashed px-6 py-10 text-center transition-colors ${dragging ? 'border-primary bg-primary/5' : 'border-border'} ${status === 'error' ? 'border-destructive/50 bg-destructive/5' : ''}`} onKeyDown={(e) => { if ((e.key === 'Enter' || e.key === ' ') && status === 'idle') inputRef.current?.click() }} onDragOver={(e) => { e.preventDefault(); setDragging(true) }} onDragLeave={() => setDragging(false)} onDrop={(e) => { e.preventDefault(); setDragging(false); chooseFile(e.dataTransfer.files[0]) }}><input ref={inputRef} id="pdf-upload" type="file" accept="application/pdf,.pdf" aria-label="Choose a PDF file" className="sr-only" onChange={(e) => chooseFile(e.target.files?.[0])} />{status === 'idle' && <><span className="mb-5 flex size-16 items-center justify-center rounded-2xl bg-secondary text-primary"><Upload className="size-7" /></span><h2 className="text-xl font-semibold">Drop your PDF here</h2><p className="mt-2 text-sm text-muted-foreground">or choose a file from your device</p><button type="button" onClick={() => inputRef.current?.click()} className="mt-7 rounded-full bg-primary px-5 py-2.5 text-sm font-medium text-primary-foreground">Browse files</button></>}{status === 'ready' && file && <><span className="mb-5 flex size-16 items-center justify-center rounded-2xl bg-primary/10 text-primary"><FileText className="size-7" /></span><h2 className="max-w-full truncate text-xl font-semibold">{file.name}</h2><p className="mt-2 text-sm text-muted-foreground">{formatSize(file.size)} · PDF ready to convert</p><div className="mt-7 flex flex-wrap justify-center gap-3"><button type="button" onClick={reset} className="rounded-full border border-border px-5 py-2.5 text-sm font-medium">Choose another</button><button type="button" onClick={convert} className="flex items-center gap-2 rounded-full bg-primary px-5 py-2.5 text-sm font-medium text-primary-foreground">Convert to DOCX <ArrowRight className="size-4" /></button></div></>}{status === 'converting' && <><RefreshCw className="mb-5 size-8 animate-spin text-primary" aria-hidden="true" /><h2 className="text-xl font-semibold" aria-live="polite">Converting your PDF</h2><p className="mt-2 text-sm text-muted-foreground">Extracting text and preparing your DOCX…</p></>}{status === 'success' && <><Check className="mb-5 size-10 rounded-full bg-primary p-2 text-primary-foreground" /><h2 className="text-xl font-semibold" aria-live="polite">Your DOCX is ready</h2><p className="mt-2 text-sm text-muted-foreground">{file?.name.replace(/\.pdf$/i, '')}.docx</p><div className="mt-7 flex flex-wrap justify-center gap-3"><button type="button" onClick={reset} className="rounded-full border border-border px-5 py-2.5 text-sm font-medium">Convert another</button><a href={downloadUrl} download={`${file?.name.replace(/\.pdf$/i, '')}.docx`} className="flex items-center gap-2 rounded-full bg-primary px-5 py-2.5 text-sm font-medium text-primary-foreground">Download DOCX <Download className="size-4" /></a></div></>}{status === 'error' && <><X className="mb-5 size-10 rounded-full bg-destructive/10 p-2 text-destructive" /><h2 className="text-xl font-semibold">We couldn&apos;t use that file</h2><p role="alert" className="mt-2 max-w-sm text-sm leading-6 text-muted-foreground">{error}</p><button type="button" onClick={reset} className="mt-7 rounded-full bg-primary px-5 py-2.5 text-sm font-medium text-primary-foreground">Try another PDF</button></>}</div><p className="flex items-center justify-center gap-2 px-4 pb-1 pt-4 text-xs text-muted-foreground"><LockKeyhole className="size-3" /> Files are processed in memory</p></div> }
function Feature({ icon, title, copy }: { icon: React.ReactNode; title: string; copy: string }) { return <article className="flex flex-col gap-4 rounded-2xl border border-border bg-card p-6"><span className="flex size-10 items-center justify-center rounded-xl bg-secondary text-primary">{icon}</span><h3 className="font-semibold">{title}</h3><p className="text-sm leading-6 text-muted-foreground">{copy}</p></article> }
